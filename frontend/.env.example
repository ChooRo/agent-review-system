# Recommended for Vite development: same-origin /api is proxied below.
VITE_API_BASE_URL=/api/v1
# Optional Vite dev proxy target. Keep it on 127.0.0.1 to avoid another localhost service.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
